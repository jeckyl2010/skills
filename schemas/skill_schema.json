{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Skill frontmatter",
  "description": "YAML frontmatter schema for SKILL.md files",
  "type": "object",
  "required": ["name", "description", "version", "tags", "authors"],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Kebab-case skill name, must match directory name"
    },
    "description": {
      "type": "string",
      "maxLength": 150,
      "description": "One-line description of what the skill does"
    },
    "version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+(\\.[0-9]+)?$",
      "description": "Semver version string"
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Tags for discovery and filtering"
    },
    "authors": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of authors"
    },
    "specificity": {
      "type": "string",
      "enum": ["generic", "stack-specific", "context-specific"],
      "description": "Scope of the skill: generic (language-agnostic), stack-specific (tied to a tech or toolchain), or context-specific (tied to a domain or project)"
    },
    "parent": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Name of the parent skill this extends (kebab-case). Child skills are loaded alongside their parent."
    },
    "triggers": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Keywords signalling this child skill is relevant. Valid only on child skills (specificity: stack-specific or context-specific)."
    },
    "tool_agnostic": {
      "type": "boolean",
      "description": "True if the skill body contains no tool-specific syntax or provider references"
    },
    "tested_on": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Models this skill has been verified on, e.g. ['claude-sonnet-4.6 (2026-05-23)']"
    },
    "deprecated_since": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+(\\.[0-9]+)?$",
      "description": "Version from which this skill is deprecated. Triggers a validator warning."
    },
    "superseded_by": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Name of the skill that replaces this one (kebab-case)"
    }
  }
}
